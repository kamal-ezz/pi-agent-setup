{
  "name": "pi-persistent-goal",
  "private": true,
  "type": "module",
  "scripts": {
    "link-host-deps": "node link-host-deps.mjs",
    "check": "npm run link-host-deps --silent && tsc --noEmit -p .",
    "test": "npm run link-host-deps --silent && node --test --experimental-strip-types *.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
