{
  "name": "mcp-runtime",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "link-host-deps": "node link-host-deps.mjs",
    "check": "npm run link-host-deps --silent && tsc --noEmit -p .",
    "test": "node --test --experimental-strip-types inventory.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
